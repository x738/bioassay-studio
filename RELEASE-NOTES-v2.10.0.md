# BioAssay Studio v2.10.0

## 本次重点

- 多页 TIFF：打开单图、内参/目的蛋白双图时可选择实际分析页面；批处理自动选择像素最多的页面并记录页码。
- TIFF 兼容：保留 8/16-bit 原始像素路径，增加 Deflate 解压组件；尝试读取 LSM；对 BigTIFF、CZI 和不支持的像素布局给出明确转换建议。
- WB 批处理：可一次选择多张 WB 图，顺序解码、自动生成候选 ROI、汇总 QC，并导出候选级 CSV；每张图仍可打开到单图工作区人工复核。
- 跨模块保存：qPCR 结果和目的蛋白/内参归一化结果可保存到离线实验知识库的计算历史。
- 自动化保护：核心分析、实验配方计算和离线资源检查纳入 GitHub Actions；真实 WB 图片继续只在本机回归，不上传私人实验数据。

## 审查问题 1–9 状态

| 编号 | 状态 | 说明 |
|---|---|---|
| 1 自动化测试 | 基本完成 | 核心纯函数与离线资源加入 CI；真实图片 GUI 回归保留在本机。 |
| 2 拆分 app.js | 未完成 | 已有 analysis-core.js、experiment-core.js，但 UI/状态层仍过大，留给 v3.0 风险隔离重构。 |
| 3 ImageJ 式引导区 | 完成 | 可手动画搜索区，自动识别只在指定范围内运行。 |
| 4 条带边界精修 | 完成 | 粗定位后回到原始像素精修边界，并保留手动调整。 |
| 5 降采样精度 | 完成 | 按图像类型提高分析副本上限，并在原始分辨率收边。 |
| 6 TIFF 支持 | 本轮完成 | 多页、Deflate、LSM 尝试、BigTIFF/CZI 明确错误；CZI 仍需先转 TIFF。 |
| 7 弱带阈值 | 完成 | 多排预设采用较宽松阈值；未采用会破坏真实图回归的激进全局降阈值。 |
| 8 模块间数据流 | 本轮完成 | qPCR、双图 WB 结果可直接进入知识库计算历史。 |
| 9 批处理 | 本轮完成 | 多图顺序初筛、QC 汇总、逐张复核和 CSV 导出。 |

## 科学使用边界

- 自动识别和批处理结果均为候选初筛，导出或用于定量前必须人工确认条带身份、ROI 完整性、背景区域与曝光饱和度。
- 已饱和图像不能通过软件恢复真实灰度信息；需要使用未饱和曝光重新定量。
- 12-bit packed、BigTIFF 和 CZI 不是当前浏览器解码器的完整支持范围，建议从成像软件导出为无损灰度 16-bit TIFF。

# BioAssay Studio v2.16.2

v2.16.2 是开始新一轮 UI/UX 重构前的稳定功能基线。旧版 GitHub Release 和标签继续保留，不会被覆盖。

## 本版重点

- 实验配方增加“实验体系 → 配方类型 → 用途版本”三级分类，允许同一实验（如 Co-IP）保存不同目的、不同严格度的多套配方。
- 配方组成支持从 Excel、WPS、CSV 或制表符表格直接粘贴，自动识别成分、浓度、单位与分子量，并计算实际加入量。
- 实验方法可直接选择并插入已有配方，同时显示用途、目标体积和实际加入量；配方更新后引用内容同步更新。
- 实验知识库调整为更适合电脑浏览器的两栏工作区，记录属性默认折叠，减少遮挡和横向滚动。
- 保留现有 qPCR、WB、考马斯亮蓝定量、实验知识库、离线 OCR、项目文件与导出功能。

## 下载

- `BioAssay-Studio-v2.16.2-Windows-x64-Setup.exe`：Windows 10/11 x64 安装版。
- `BioAssay-Studio-Web-v2.16.2.zip`：离线网页包，解压后打开 `index.html`。
- `latest.yml` 和 `.blockmap`：Windows 客户端检查更新所需文件。

## 数据与升级

- 实验文件和数据库仍在本机处理。
- 安装新版默认不会删除旧版创建的本地数据。
- 重要配方、实验方法和分析项目建议升级前先导出备份。

